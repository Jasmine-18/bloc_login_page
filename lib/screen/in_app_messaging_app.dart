import 'package:flutter/material.dart';

class InAppMessagingPage extends StatelessWidget {
  const InAppMessagingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
